version: '3'

services:
  cmake:
    container_name: bel
    build:
      context: .
      dockerfile: Dockerfile

    tty: true
    command: ./rmsdapp

    volumes:
      - ./src:/usr/src
      - $HOME/go:/root/go
    ports:
      - "8080:8080"
